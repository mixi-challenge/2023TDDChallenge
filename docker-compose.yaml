version: '3'
services:
  tdd:
    build: .
    user: ruby
    tty: true
    stdin_open: true
    volumes:
      - .:/src/tdd_challenge
      - bundle:/usr/local/bundle
      - /src/.git

volumes:
  bundle:
    driver: local
